<nav class="navbar navbar-default">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navigation">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            </button>
            <% if user_signed_in? %>
                <a class="navbar-brand" href="/">一覧ページへ</a>
            <% end %>
        </div>
        <div class="collapse navbar-collapse float-right" id="navigation">
            <% if user_signed_in? %>
                <div class="flex margin-top-7">
                    <div>
                        <%= link_to '新規登録画面へ', new_history_path, class: 'btn btn-info margin-right-10' %>
                    </div>
                    <div class="form-group">
                        <%= form_tag(search_path, :method => 'GET', class: 'form-inline' ) do %>
                        <%= text_field_tag :search, '', placeholder: "名前を入力してください", class: 'form-control', id:  'search-form' %>
                        <%= submit_tag '検索', class: 'btn btn-primary search-button', name: nil %>
                        <% end %>
                    </div>
                    
                    <div>
                        <%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: 'btn btn-warning' %>
                    </div>
                </div>
            <% end %>
            
        </div>
    </div>
</nav>